source "https://rubygems.org"
git_source(:gitlab) {|repo_name| "https://gitlab-dev.dev.tc.apps/#{repo_name}" }

gem "date", require: true
gem "faraday", require: true
gem "json", require: true
gem "numo-gnuplot", require: ["numo/gnuplot"]
gem "csvreader", require: ["csvreader"]
gem "pry", require: true

